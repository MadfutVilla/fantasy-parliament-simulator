# Fantasy Parliament Simulator

Ready-to-upload Vite React project. Upload this folder to Vercel or connect it to GitHub.

## Vercel
Drag/drop the folder, or import the GitHub repo. Build command: `npm run build`. Output directory: `dist`.
